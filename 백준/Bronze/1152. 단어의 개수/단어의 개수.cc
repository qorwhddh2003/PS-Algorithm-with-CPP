#include <bits/stdc++.h>
using namespace std;

int ret;
string s;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    while(cin >> s){
       ret++;
    }
    cout << ret;
    return 0;
}