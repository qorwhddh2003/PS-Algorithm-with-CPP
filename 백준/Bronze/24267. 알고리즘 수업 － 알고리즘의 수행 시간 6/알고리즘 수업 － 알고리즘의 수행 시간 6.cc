#include <bits/stdc++.h>
using namespace std;

long long n;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> n;
    cout << (n - 2) * (n - 1) / 2 * n / 3 << "\n" << 3;
    return 0;
}
